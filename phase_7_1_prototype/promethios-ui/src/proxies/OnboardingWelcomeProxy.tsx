import React from 'react';
import OnboardingWelcome from '../../../ui/onboarding/OnboardingWelcome';

/**
 * OnboardingWelcomeProxy Component
 * 
 * This proxy component serves as a bridge to the OnboardingWelcome component in the /ui/ directory.
 * It provides the LangSmith-style "How would you like to start?" interface with workflow options.
 */
const OnboardingWelcomeProxy: React.FC = () => {
  return <OnboardingWelcome />;
};

export default OnboardingWelcomeProxy;oardingWelcomeProxy;
