import DeployPage from '../components/pages/DeployPage';

/**
 * Deploy Proxy Component
 * 
 * This proxy component serves as a bridge to the Deploy page component.
 * It provides the deployment and export functionality for governed agents.
 */
const DeployProxy: React.FC = () => {
  return <DeployPage />;
};

export default DeployProxy;

