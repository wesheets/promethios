import React from 'react';
import SettingsPage from '../components/pages/SettingsPage';

/**
 * Settings Proxy Component
 * 
 * This proxy component serves as a bridge to the Settings page component.
 * It provides user settings and preferences management functionality.
 */
const SettingsProxy: React.FC = () => {
  return <SettingsPage />;
};

export default SettingsProxy;
